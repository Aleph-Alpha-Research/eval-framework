# Aleph Alpha Eval-Framework Docs

Welcome to the Docs for the Eval Framework. The docs can be built using `mkdocs`.

1. Install `mkdocs`
```
pip install mkdocs
```

2. Start the server
```
mkdocs serve
```